# Copy to .env and tweak for your machine. Do not commit .env.
# These are read by docker-compose only.

POSTGRES_USER=giwu
POSTGRES_PASSWORD=giwu_dev_pw

PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin
