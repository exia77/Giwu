namespace Giwu.Domain.Organization;

public enum Gender { Male, Female, Other, PreferNotToSay }
public enum CivilStatus { Single, Married, Widowed, Separated, Divorced }
public enum EmploymentStatus { Active, OnLeave, Suspended, Terminated, Resigned }
public enum EmploymentType { Regular, Probationary, Contractual, Project, Consultant, Intern }
public enum WorkArrangement { Onsite, Remote, Hybrid }
public enum PayFrequency { Monthly, SemiMonthly, BiWeekly, Weekly }
